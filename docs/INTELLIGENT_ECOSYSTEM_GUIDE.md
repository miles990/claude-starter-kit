# Claude Intelligent Ecosystem 完整使用指南

> 讓 5 個專案協同工作，打造自我進化的 AI 開發環境

---

## 目錄

1. [生態系統概覽](#1-生態系統概覽)
2. [快速開始](#2-快速開始)
3. [核心概念](#3-核心概念)
4. [skillpkg 使用指南](#4-skillpkg-使用指南)
5. [Self-Evolving Agent 深度指南](#5-self-evolving-agent-深度指南)
6. [Domain Skills 整合](#6-domain-skills-整合)
7. [Software Skills 整合](#7-software-skills-整合)
8. [進階功能](#8-進階功能)
9. [最佳實踐](#9-最佳實踐)
10. [故障排除](#10-故障排除)

---

## 1. 生態系統概覽

### 1.1 五個專案的關係

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Claude Intelligent Ecosystem                        │
│                                                                          │
│    ┌─────────────────────────────────────────────────────────────────┐  │
│    │                    claude-starter-kit                            │  │
│    │                    (環境整合中心)                                 │  │
│    │  • CLAUDE.md 配置                                                │  │
│    │  • .claude/rules/ 規則                                           │  │
│    │  • .claude/memory/ 記憶                                          │  │
│    │  • skillpkg.json 依賴                                            │  │
│    └───────────────────────────┬─────────────────────────────────────┘  │
│                                │                                         │
│         ┌──────────────────────┼──────────────────────┐                 │
│         │                      │                      │                 │
│         ▼                      ▼                      ▼                 │
│    ┌─────────────┐      ┌─────────────┐      ┌─────────────┐           │
│    │  skillpkg   │◄────►│self-evolving│      │   MCP 工具   │           │
│    │ (包管理器)   │      │   agent     │      │ (能力擴展)   │           │
│    │ 12 MCP 工具 │      │ (執行引擎)  │      │ context7 等  │           │
│    └──────┬──────┘      └──────┬──────┘      └─────────────┘           │
│           │                    │                                         │
│           │      安裝/載入      │    觸發/使用                           │
│           ▼                    ▼                                         │
│    ┌─────────────────────────────────────────────────────────────────┐  │
│    │                    Skill 生態系統                                │  │
│    │  ┌─────────────────────┐    ┌─────────────────────┐            │  │
│    │  │ claude-domain-skills│    │claude-software-skills│            │  │
│    │  │     (16 領域技能)    │    │    (49 軟體技能)     │            │  │
│    │  │ Finance, Business,  │    │ Design, Engineering, │            │  │
│    │  │ Creative, Pro, Life │    │ Stacks, Tools, Lang  │            │  │
│    │  └─────────────────────┘    └─────────────────────┘            │  │
│    └─────────────────────────────────────────────────────────────────┘  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 核心價值

| 層級 | 專案 | 價值 |
|------|------|------|
| 基礎設施 | skillpkg | 統一的技能安裝、管理、搜尋 |
| 執行引擎 | self-evolving-agent | 自主目標達成、PDCA 循環 |
| 領域知識 | claude-domain-skills | 16 個非技術領域的專業知識 |
| 技術知識 | claude-software-skills | 49 個軟體開發技能模組 |
| 整合模板 | claude-starter-kit | 開箱即用的最佳配置 |

### 1.3 版本兼容性

```json
{
  "ecosystem_version": "1.0.0",
  "tested_combinations": {
    "skillpkg": ">=0.5.5",
    "self-evolving-agent": ">=3.6.0",
    "claude-domain-skills": ">=1.1.0",
    "claude-software-skills": ">=1.0.0"
  }
}
```

---

## 2. 快速開始

### 2.1 一鍵設置（推薦）

```bash
# 在任何專案中執行
cd your-project
npx skillpkg-cli init --preset=standard --install
```

**Preset 選項：**

| Preset | 包含內容 | 適合場景 |
|--------|---------|---------|
| `minimal` | CLAUDE.md + 基本規則 | 快速體驗 |
| `standard` | + Memory 系統 + self-evolving-agent | 一般開發 |
| `full` | + software-skills (49 模組) | 完整功能 |

### 2.2 手動設置

```bash
# 1. 建立目錄結構
mkdir -p .claude/{rules,skills,memory/{learnings,decisions,failures,patterns,strategies}}

# 2. 創建 CLAUDE.md
cat > CLAUDE.md << 'EOF'
# My Project

## Quick Reference
- `/evolve [目標]` - 觸發自動進化

## Skills
@.claude/skills/self-evolving-agent/SKILL.md

## Rules
@.claude/rules/
EOF

# 3. 安裝核心技能
npx skillpkg-cli install github:miles990/self-evolving-agent

# 4. (可選) 安裝領域技能
npx skillpkg-cli install github:miles990/claude-domain-skills
```

### 2.3 驗證安裝

```bash
# 檢查已安裝的技能
npx skillpkg-cli list

# 應該看到：
# ✓ self-evolving-agent (3.7.0)
# ✓ claude-domain-skills (1.1.0) [如果安裝了]
```

### 2.4 開始使用

在 Claude Code 中：

```bash
# 觸發自我進化
/evolve 建立一個 REST API，包含用戶認證功能

# 搜尋記憶
Grep pattern="authentication" path=".claude/memory/"
```

---

## 3. 核心概念

### 3.1 Skill 的本質

**Skill 不只是知識，是「封裝好的判斷力」**

```
┌─────────────────────────────────────────────────────────────────┐
│  傳統程式設計              AI 時代                              │
│  ───────────────          ─────────                             │
│  人類 → 程式語言 → 電腦    人類 → 抽象介面 → AI → 執行          │
│        ↓                         ↓                              │
│      精確控制               意圖傳達 + 能力授權                  │
└─────────────────────────────────────────────────────────────────┘

對應關係：
┌──────────────┬──────────────┬────────────────────────┐
│ 傳統軟體     │ AI 協作      │ 作用                   │
├──────────────┼──────────────┼────────────────────────┤
│ API          │ MCP          │ 能力邊界（能做什麼）   │
│ SDK/Library  │ Tools        │ 具體實作（怎麼做）     │
│ 文檔+實踐    │ Skill        │ 領域知識（何時用什麼） │
│ Config       │ CLAUDE.md    │ 上下文約束（專案規範） │
└──────────────┴──────────────┴────────────────────────┘
```

### 3.2 Memory 系統

```
.claude/memory/
├── index.md              # 快速索引（必須維護）
├── learnings/            # 成功的解決方案
├── decisions/            # 架構決策記錄 (ADR)
├── failures/             # 失敗經驗（避免重蹈覆轍）
├── patterns/             # 可複用的推理模式
├── strategies/           # 任務特定的策略池
├── guards/               # 失敗免疫規則
└── skill-metrics/        # 技能組合使用統計
```

### 3.3 PDCA 循環

```
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│   Plan   │→ │    Do    │→ │  Check   │→ │   Act    │
│  (規劃)  │  │  (執行)  │  │ (失敗診斷)│  │ (改進)   │
└──────────┘  └──────────┘  └──────────┘  └────┬─────┘
      ↑                                        │
      └─────────── 多策略選擇 ←─────────────────┘
```

---

## 4. skillpkg 使用指南

### 4.1 CLI 命令一覽

```bash
# 搜尋技能
skillpkg search "code review"        # 搜尋所有來源
skillpkg search "api" --source=local # 只搜本地

# 安裝技能
skillpkg install miles990/self-evolving-agent           # 從 GitHub
skillpkg install github:user/repo#skills/my-skill       # 子目錄
skillpkg install gist:abc123                            # 從 Gist
skillpkg install https://example.com/SKILL.md           # 從 URL

# 管理技能
skillpkg list                        # 列出已安裝
skillpkg info <skill>                # 查看詳情
skillpkg load <skill>                # 載入 instructions
skillpkg uninstall <skill>           # 移除

# 同步到平台
skillpkg sync                        # 同步到所有平台
skillpkg sync --target=claude-code   # 只同步到 Claude Code
```

### 4.2 MCP 工具（AI 自主使用）

skillpkg 提供 12 個 MCP 工具，讓 AI 能自主學習：

| 工具 | 用途 | 使用時機 |
|------|------|---------|
| `list_skills` | 列出已安裝技能 | 了解當前能力 |
| `load_skill` | 載入技能內容 | 需要特定知識時 |
| `install_skill` | 安裝新技能 | 發現能力缺口時 |
| `uninstall_skill` | 移除技能 | 清理不需要的 |
| `search_skills` | 搜尋可用技能 | 探索新能力 |
| `recommend_skill` | 單一推薦 | 快速查詢 |
| `recommend_skills` | 智能推薦 | 根據目標分析 |
| `skill_info` | 技能詳情 | 了解特定技能 |
| `skill_status` | 整體狀態 | 健康檢查 |
| `sync_skills` | 同步到平台 | 保持一致 |
| `create_skill` | 創建新技能 | 知識蒸餾 |
| `fetch_skill_content` | 預覽遠端技能 | 安裝前評估 |

### 4.3 智能推薦系統

`recommend_skills` 是最強大的工具：

```
輸入: "建立一個量化交易回測系統"
       ↓
┌─────────────────────────────────────────────────────────────────┐
│  🎯 分析您的目標後，推薦以下 skills：                           │
│                                                                 │
│  📦 Domain Skills (2):                                         │
│     ✓ quant-trading (85% confidence)                           │
│       Matched: 量化, 交易, 回測                                 │
│     ✓ investment-analysis (72% confidence)                     │
│       Matched: 量化                                             │
│                                                                 │
│  🔧 Software Skills (4):                                       │
│     ✓ python (90% confidence)                                  │
│     ✓ database (78% confidence)                                │
│     ✓ api-design (65% confidence)                              │
│     ○ testing-strategies (建議)                                │
│                                                                 │
│  📋 From Dependencies:                                          │
│     python, database (quant-trading 的依賴)                    │
└─────────────────────────────────────────────────────────────────┘
```

### 4.4 Research Mode

當信心分數 < 50% 時自動觸發：

```
⚠️ Research Mode Triggered (confidence < 50%)

現有 skills 匹配度較低，建議：
1. 使用 search_skills 搜尋 GitHub
2. 安裝相關 skills 後重試
3. 或繼續執行，在過程中學習
```

---

## 5. Self-Evolving Agent 深度指南

### 5.1 觸發方式

```bash
/evolve [目標描述]

# 範例：
/evolve 建立一個能自動生成遊戲道具圖片的 ComfyUI 工作流程
/evolve 優化這段程式碼的效能，目標是降低 50% 執行時間
/evolve 為這個專案建立完整的測試覆蓋率達到 80%
```

### 5.2 執行流程

```
/evolve [目標]
     │
     ▼
┌─────────────────────────────────────────────────────────────────┐
│  Phase -1: PSB 環境檢查                                         │
│  □ Git Repo 已建立？                                            │
│  □ CLAUDE.md 存在？                                             │
│  □ .claude/memory/ 已初始化？                                   │
│  □ MCP 配置正確？                                               │
└─────────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────────┐
│  Phase 1: 目標分析                                              │
│  • 解析目標，確認驗收標準                                       │
│  • 若目標不明確，詢問用戶                                       │
│  • 分解子目標，識別依賴關係                                     │
└─────────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────────┐
│  Phase 1.5: 能力邊界評估                                        │
│  • 評估需要哪些技能                                             │
│  • Grep .claude/memory/ 查找經驗                               │
│  • recommend_skills 分析目標                                    │
│  • 若缺少技能 → install + load                                 │
│  • 若信心不足 → Research Mode                                   │
└─────────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────────┐
│  Phase 2: PDCA 執行循環                                         │
│                                                                 │
│  Plan → Do → Check → Act                                       │
│   ↑                    │                                        │
│   └────── 多策略重試 ──┘                                        │
│                                                                 │
│  失敗類型：                                                     │
│  A: 知識缺口 → recommend_skill → install → learn              │
│  B: 執行錯誤 → 重新閱讀文檔                                    │
│  C: 環境問題 → 修復環境                                        │
│  D: 策略錯誤 → 切換策略池                                      │
│  E: 資源限制 → 優化或分批                                      │
└─────────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────────┐
│  Phase 3: 記憶更新                                              │
│  • 成功經驗 → learnings/                                       │
│  • 失敗教訓 → failures/                                        │
│  • 架構決策 → decisions/                                       │
│  • 技能組合 → skill-metrics/                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 強制檢查點（不可跳過）

```
┌─────────────────────────────────────────────────────────────────┐
│  Checkpoint 1: 任務開始前 - 主動查 Memory                       │
│                                                                 │
│  □ grep -r "關鍵字" .claude/memory/                            │
│  □ 有找到 → 閱讀並應用                                         │
│  □ 沒找到 → 記錄「無相關經驗」，繼續執行                       │
│                                                                 │
│  ❌ 禁止：不查 memory 就開始執行                                │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Checkpoint 2: 程式碼變更後 - 編譯 + 測試                       │
│                                                                 │
│  □ 編譯通過（npm run build / tsc）                             │
│  □ 相關測試通過（npm test）                                    │
│                                                                 │
│  ❌ 禁止：編譯失敗還繼續下一個任務                              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Checkpoint 3: Milestone 完成後 - 目標確認                      │
│                                                                 │
│  1. 現在的目標是什麼？                                          │
│  2. 方向有沒有偏離目標？                                        │
│  3. 下一步是什麼？                                              │
│                                                                 │
│  ❌ 禁止：用戶說「繼續」就盲目繼續                              │
└─────────────────────────────────────────────────────────────────┘
```

### 5.4 進度報告格式

```
📊 進度更新

目標：建立 ComfyUI 工作流程
進度：███████░░░ 70%

✅ 已完成：
   - 安裝 ComfyUI
   - 下載基礎模型
   - 建立基本工作流程

🔄 進行中：
   - 加入 LoRA 支援

⏳ 待完成：
   - 批量生成功能
   - 輸出格式優化

📝 學習記錄：
   - 發現：LoRA 需要特定節點
   - 解決：安裝 ComfyUI-Manager
   - 已儲存到：.claude/memory/learnings/
```

---

## 6. Domain Skills 整合

### 6.1 可用領域（16 個）

| 類別 | 技能 | 觸發詞 |
|------|------|--------|
| 💼 Business | marketing | 行銷, SEO, 內容策略 |
|  | sales | 銷售, 轉換, CRM |
|  | product-management | PRD, 用戶故事, OKR |
|  | project-management | Scrum, Sprint, 敏捷 |
|  | strategy | 商業策略, SWOT |
| 💰 Finance | quant-trading | 量化, 回測, 交易策略 |
|  | investment-analysis | 財報, 估值, 投資 |
| 🎨 Creative | game-design | 遊戲機制, 關卡設計 |
|  | ui-ux-design | 介面, 用戶體驗 |
|  | storytelling | 劇情, 角色, 敘事 |
|  | brainstorming | 創意, 發想, 腦力激盪 |
|  | visual-media | 圖片, 影片, 視覺 |
| 🔬 Professional | research-analysis | 市場研究, 數據分析 |
|  | knowledge-management | PARA, Zettelkasten |
| 🌱 Lifestyle | personal-growth | 時間管理, 職涯 |
|  | side-income | 副業, 被動收入 |

### 6.2 自動領域識別

self-evolving-agent 會自動從目標中提取關鍵詞：

```
目標：「建立一個量化交易回測系統」

關鍵詞提取：量化, 交易, 回測, 系統
     ↓
匹配 triggers
     ↓
自動載入：quant-trading, investment-analysis
```

### 6.3 Domain → Tech 接口

每個領域技能都有對應的技術映射：

```yaml
# finance → tech 接口範例
domain_requirement: "財務數據分析"
technical_implementation: "Python + Pandas/NumPy"
software_skills: ["python", "data-analysis"]

domain_requirement: "即時行情處理"
technical_implementation: "WebSocket + 時序 DB"
software_skills: ["realtime-systems", "database"]
```

---

## 7. Software Skills 整合

### 7.1 技能分類（49 個）

| 類別 | 數量 | 範例 |
|------|------|------|
| 軟體設計 | 6 | architecture-patterns, api-design |
| 軟體工程 | 7 | testing-strategies, devops-cicd |
| 開發堆棧 | 8 | frontend, backend, database |
| 工具整合 | 6 | git-workflows, project-management |
| 領域應用 | 8 | e-commerce, saas-platforms |
| 程式語言 | 12 | python, javascript-typescript, go |

### 7.2 Skill Recipes（黃金組合）

已驗證的高效組合：

**量化交易系統**
```yaml
domain: [quant-trading, investment-analysis]
software: [python, database, api-design, testing-strategies]
phases:
  1. 策略研究: investment-analysis → quant-trading
  2. 資料管道: database → api-design
  3. 策略實現: python → testing-strategies
```

**SaaS 電商平台**
```yaml
domain: [product-management, marketing, sales]
software: [saas-platforms, e-commerce, frontend, backend, database]
phases:
  1. 產品規劃: product-management
  2. 架構設計: saas-platforms → e-commerce
  3. 開發實現: frontend + backend + database
  4. 成長執行: marketing + sales
```

**遊戲開發全流程**
```yaml
domain: [game-design, storytelling, ui-ux-design]
software: [game-development, frontend, realtime-systems]
phases:
  1. 概念設計: game-design + storytelling
  2. 視覺設計: ui-ux-design
  3. 核心開發: game-development
  4. 網路功能: realtime-systems
```

### 7.3 Skill 強化功能（v1.1 新增）

Skills 現在支援三個強化功能，讓 AI 更聰明：

#### Sharp Edges（陷阱警告）

每個 skill 可定義該領域的常見陷阱：

```markdown
## Sharp Edges（常見陷阱）

### SE-1: 空的 Promise rejection 處理
- **嚴重度**: critical
- **情境**: 使用 async/await 但沒有處理 rejection
- **症狀**: 程式靜默失敗、錯誤被吞掉
- **檢測**: `\.catch\s*\(\s*\)|\\.catch\s*\(\s*\(\s*\)\s*=>`
- **解法**: 至少 console.error(err) 或 throw err
```

**作用**：在 PDCA Plan 階段，AI 會搜尋載入的 skills 的 sharp_edges，主動警告可能觸發的陷阱。

#### Validations（代碼驗證）

每個 skill 可定義可執行的驗證規則：

```markdown
## Validations

### V-1: 禁止空的 catch block
- **類型**: regex
- **嚴重度**: critical
- **模式**: `catch\s*\([^)]*\)\s*\{\s*\}`
- **訊息**: Empty catch block silently swallows errors
- **修復建議**: Add error logging: `console.error(err)`
- **適用**: `*.ts`, `*.js`
```

**作用**：在 PDCA Check 階段，AI 會用這些 regex/ast 模式掃描變更的程式碼，報告違規並提供修復建議。

#### Collaboration（技能協作）

Skills 現在可以聲明與其他 skills 的協作關係：

```yaml
# 在 SKILL.md frontmatter 中
collaboration:
  prerequisites:
    - skill: typescript
      reason: Type-safe development
  delegation_triggers:
    - trigger: API endpoint design
      delegate_to: api-design
      context: RESTful conventions
  receives_context_from:
    - skill: api-design
      receives:
        - Endpoint naming conventions
        - Error response formats
  provides_context_to:
    - skill: frontend
      provides:
        - API endpoints list
        - Authentication flow
```

**作用**：
1. **Plan 階段**：檢查 delegation_triggers，建議載入相關 skill
2. **recommend_skills**：考慮 prerequisites，自動包含依賴技能
3. **上下文傳遞**：技能之間明確定義傳遞什麼資訊

#### 協作圖範例

```
                    ┌─────────────┐
                    │ typescript  │
                    └──────┬──────┘
                           │ prerequisite
                           ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  api-design │────▶│   backend   │────▶│  frontend   │
└─────────────┘     └──────┬──────┘     └─────────────┘
      provides              │ delegates
      context               ▼
                    ┌─────────────┐
                    │  database   │
                    └─────────────┘
```

---

## 8. 進階功能

### 8.1 知識蒸餾

當 `.claude/memory/learnings/` 累積 3+ 筆相似主題時，系統會提議生成新 Skill：

```
📝 發現可蒸餾的知識模式

在 .claude/memory/learnings/ 中找到 5 筆關於「React 效能優化」的記錄

建議生成新 Skill：
  名稱: react-performance-patterns
  描述: React 效能優化的完整模式集合

要生成這個 Skill 嗎？[Y/n/e(edit)]
```

### 8.2 失敗免疫

從失敗經驗自動生成「Guard」：

```yaml
# .claude/memory/guards/api-timeout-guard.md
trigger_pattern: "使用外部 API|HTTP 請求|fetch"
severity: high
check:
  - 是否設定 timeout？
  - 是否有 retry 邏輯？
  - 是否有錯誤處理？
```

在 Phase 1 時自動檢查相關 Guards。

### 8.3 涌現追蹤

記錄技能組合使用情況：

```yaml
# .claude/memory/skill-metrics/combinations/2025-01.yaml
- id: "combo-001"
  task: "建立量化回測系統"
  skills_used:
    domain: [quant-trading, investment-analysis]
    software: [python, database]
  success: true
  iterations: 3

summary:
  most_effective: [python, database, api-design]  # 92% 成功率
```

### 8.4 遺忘曲線

Memory 條目追蹤 relevance_score：

```yaml
access_log:
  created: 2025-01-07
  last_accessed: 2025-01-15
  access_count: 8
  relevance_score: 0.85  # 動態計算

# 衰減規則
# 每 30 天未訪問: -0.1
# 每次訪問: +0.05
# < 0.3: 建議刪除
```

---

## 9. 最佳實踐

### 9.1 每日工作流程

```
開始工作
    │
    ▼
搜尋相關記憶 ─────────────┐
Grep "keyword" .claude/memory/
    │                     │
    ▼                     ▼
有相關經驗？ ──Yes──► 閱讀並應用
    │
    No
    │
    ▼
執行任務
    │
    ▼
學到新東西？ ──Yes──► 創建記憶
    │                   ↓
    No              更新 index.md
    │
    ▼
結束
```

### 9.2 Memory 維護

**每週：**
- 快速瀏覽新增的 learnings
- 合併相似條目

**每月：**
- 審查 relevance_score < 0.3 的條目
- 標註過時的為 `status: deprecated`
- 更新 index.md 的 metadata

### 9.3 Skill 使用

**DO：**
- 讓 AI 自主搜尋和安裝需要的技能
- 使用 `recommend_skills` 根據目標分析
- 記錄有效的技能組合

**DON'T：**
- 手動安裝所有技能（過度負載）
- 忽略 Research Mode 建議
- 跳過記憶搜尋步驟

---

## 10. 故障排除

### 10.1 常見問題

**Q: /evolve 沒有反應**
```bash
# 檢查 skill 是否安裝
npx skillpkg-cli list | grep evolve

# 若沒有，安裝它
npx skillpkg-cli install github:miles990/self-evolving-agent
npx skillpkg-cli sync
```

**Q: recommend_skills 沒有結果**
```bash
# 檢查已安裝的技能
npx skillpkg-cli list

# 安裝更多技能
npx skillpkg-cli install github:miles990/claude-domain-skills
npx skillpkg-cli install github:miles990/claude-software-skills
```

**Q: Memory 搜尋沒有結果**
```bash
# 確認目錄存在
ls -la .claude/memory/

# 若不存在，初始化
mkdir -p .claude/memory/{learnings,decisions,failures,patterns,strategies}
```

### 10.2 重置環境

```bash
# 完全重置
rm -rf .claude/skills/
npx skillpkg-cli install
npx skillpkg-cli sync
```

### 10.3 取得幫助

- skillpkg: [github.com/miles990/skillpkg](https://github.com/miles990/skillpkg)
- self-evolving-agent: [github.com/miles990/self-evolving-agent](https://github.com/miles990/self-evolving-agent)
- claude-starter-kit: [github.com/miles990/claude-starter-kit](https://github.com/miles990/claude-starter-kit)

---

## 附錄：快速參考卡

```
┌─────────────────────────────────────────────────────────────────┐
│                   CLAUDE ECOSYSTEM CHEATSHEET                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  SETUP                                                          │
│  npx skillpkg-cli init --preset=standard --install              │
│                                                                 │
│  EVOLVE                                                         │
│  /evolve [目標描述]                                             │
│                                                                 │
│  SEARCH MEMORY                                                  │
│  Grep pattern="keyword" path=".claude/memory/"                  │
│                                                                 │
│  SKILL MANAGEMENT (CLI)                                         │
│  skillpkg search "topic"                                        │
│  skillpkg install user/repo                                     │
│  skillpkg list                                                  │
│  skillpkg sync                                                  │
│                                                                 │
│  SKILL MANAGEMENT (MCP)                                         │
│  recommend_skills({ goal: "..." })                              │
│  install_skill({ source: "..." })                               │
│  load_skill({ id: "..." })                                      │
│                                                                 │
│  MEMORY FILES                                                   │
│  .claude/memory/learnings/    成功方案                          │
│  .claude/memory/failures/     失敗教訓                          │
│  .claude/memory/decisions/    架構決策                          │
│  .claude/memory/patterns/     推理模式                          │
│  .claude/memory/guards/       失敗免疫                          │
│                                                                 │
│  PDCA LOOP                                                      │
│  Plan → Do → Check → Act → (repeat until success)              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

*最後更新：2026-01-08*
*生態系統版本：1.1.0*

**v1.1.0 新增功能：**
- Sharp Edges（陷阱警告）
- Validations（代碼驗證）
- Collaboration（技能協作）
