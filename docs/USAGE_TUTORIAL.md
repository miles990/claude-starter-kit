# Claude Starter Kit 使用教學

> 本文檔記錄如何使用 claude-software-skills 專案，可作為該專案的使用範例。

## 教學目標

通過實際操作，學習：
1. Claude Code Memory 系統的配置與使用
2. 如何整合 self-evolving-agent 和 claude-software-skills
3. 建立智能化、高效率的開發環境

---

## Part 1: 理解 Claude Code Memory 系統

### 1.1 Memory 層級結構

Claude Code 提供 **4 層記憶體**，從低到高優先級：

```
┌─────────────────────────────────────────────────────────────────┐
│  Layer 4: Enterprise Policy (最高優先級)                        │
│  位置：/Library/Application Support/ClaudeCode/CLAUDE.md       │
│  用途：組織級規範，影響所有成員                                 │
├─────────────────────────────────────────────────────────────────┤
│  Layer 3: User Memory (個人全域)                                │
│  位置：~/.claude/CLAUDE.md                                      │
│  用途：個人偏好，跨所有專案生效                                 │
├─────────────────────────────────────────────────────────────────┤
│  Layer 2: Project Memory (專案級)                               │
│  位置：./CLAUDE.md 或 ./.claude/CLAUDE.md                       │
│  用途：團隊共享的專案規範                                       │
├─────────────────────────────────────────────────────────────────┤
│  Layer 1: Project Local (個人專案級)                            │
│  位置：./CLAUDE.local.md                                        │
│  用途：個人的專案偏好（不進版控）                               │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 模組化規則 (.claude/rules/)

**Path-specific Rules** 讓規則只套用於特定檔案：

```markdown
---
paths: src/api/**/*.ts
---

# API 開發規則

這些規則只在編輯 src/api/ 下的 TypeScript 檔案時生效
```

**支援的 Glob Patterns：**
- `src/**/*.ts` - src 下所有 TypeScript
- `**/*.test.{ts,tsx}` - 所有測試檔案
- `{src,lib}/**/*.ts` - 多個目錄

### 1.3 Import 語法

使用 `@path` 引用其他文件：

```markdown
# 專案說明
詳見 @README.md

# 開發規範
參考 @docs/development-guide.md

# 個人設定
@~/.claude/my-preferences.md
```

---

## Part 2: 設置專案

### 2.1 建立專案結構

```bash
# 建立目錄
mkdir -p my-project/.claude/rules
mkdir -p my-project/.claude/skills
mkdir -p my-project/.github/memory/{learnings,decisions,failures,patterns,strategies}
mkdir -p my-project/src

cd my-project
```

### 2.2 創建 CLAUDE.md

```bash
cat > CLAUDE.md << 'EOF'
# My Project

## 專案概述
這是一個 [描述你的專案]

## 快速參考

### 常用指令
- `npm run dev` - 啟動開發伺服器
- `npm test` - 執行測試
- `/evolve [目標]` - 觸發自動進化

### 可用 Skills
- @.claude/skills/self-evolving-agent/SKILL.md
- @.claude/skills/software-skills/

## 開發規範
參見 @.claude/rules/
EOF
```

### 2.3 安裝 Skills

```bash
# 安裝 self-evolving-agent
git clone --depth 1 \
  https://github.com/miles990/self-evolving-agent.git \
  .claude/skills/self-evolving-agent

# 安裝 claude-software-skills
git clone --depth 1 \
  https://github.com/miles990/claude-software-skills.git \
  .claude/skills/software-skills
```

### 2.4 配置 MCP

```bash
cat > .mcp.json << 'EOF'
{
  "mcpServers": {
    "skillpkg": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/claude-code-mcp-skill-pkg"]
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/claude-code-mcp-context7"]
    }
  }
}
EOF
```

---

## Part 3: 使用 Self-Evolving Agent

### 3.1 基本用法

```bash
# 在 Claude Code 中執行
/evolve 建立一個 REST API，包含用戶認證功能
```

### 3.2 Agent 執行流程

```
/evolve [目標]
     │
     ▼
┌─────────────────────────────────────────┐
│  Phase 0: 初始化                        │
│  - 檢查 .github/memory/ 是否存在       │
│  - 建立記憶目錄結構                     │
└─────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  Phase 1: 目標分析                      │
│  - 解析目標，確認驗收標準               │
│  - 若目標不明確，詢問用戶               │
└─────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  Phase 1.5: 能力評估                    │
│  - 評估需要哪些技能                     │
│  - 搜尋 .github/memory/ 查找經驗        │
│  - 若缺少技能，使用 skillpkg 安裝       │
└─────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  Phase 2: PDCA 執行                     │
│  - Plan: 制定計劃                       │
│  - Do: 執行                             │
│  - Check: 檢查結果                      │
│  - Act: 若失敗，改進並重試              │
└─────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  Phase 3: 記憶更新                      │
│  - 成功經驗 → learnings/                │
│  - 失敗教訓 → failures/                 │
│  - 架構決策 → decisions/                │
└─────────────────────────────────────────┘
```

### 3.3 實際範例

**輸入：**
```
/evolve 為現有專案添加單元測試，覆蓋率達到 80%
```

**Agent 執行過程：**
```
📋 目標分析完成

目標：添加單元測試，覆蓋率 80%
驗收標準：
- Jest 測試框架配置完成
- 核心功能有測試覆蓋
- coverage report 顯示 ≥80%

🔍 搜尋過去經驗...
Grep pattern="jest|testing|coverage" path=".github/memory/"
→ 找到：learnings/2025-01-05-jest-setup.md

📚 應用過去經驗：Jest 配置最佳實踐

🔄 執行子目標 1：配置 Jest
Plan：安裝 jest, @types/jest, ts-jest
Do：npm install -D jest @types/jest ts-jest
Check：✅ 安裝成功

🔄 執行子目標 2：創建測試檔案
...

📊 進度更新
進度：████████░░ 80%

✅ 已完成：
- Jest 配置
- 核心函數測試

🔄 進行中：
- API 端點測試

💾 記錄經驗
Write(.github/memory/learnings/2025-01-07-testing-setup.md)
```

---

## Part 4: 記憶系統使用

### 4.1 搜尋記憶

```bash
# 搜尋所有記憶
Grep pattern="關鍵字" path=".github/memory/"

# 只搜尋學習記錄
Grep pattern="react" path=".github/memory/learnings/"

# 搜尋失敗經驗
Grep pattern="memory leak" path=".github/memory/failures/"
```

### 4.2 創建學習記錄

```markdown
# .github/memory/learnings/2025-01-07-example.md

---
date: 2025-01-07
tags: [tag1, tag2, tag3]
task: 描述你在做什麼
status: resolved
---

# 標題

## 情境
遇到這個問題的背景...

## 問題
具體的問題描述...

## 解決方案
1. 步驟一
2. 步驟二
3. 步驟三

## 驗證
✅ 問題已解決

## 注意事項
- 注意點 1
- 注意點 2
```

### 4.3 維護 index.md

每次新增記憶後，更新索引：

```markdown
<!-- LEARNINGS_START -->
- [新標題](learnings/2025-01-07-example.md) - tag1, tag2
<!-- LEARNINGS_END -->
```

---

## Part 5: 整合 claude-software-skills

### 5.1 查看可用技能

安裝後，47 個技能模組可用：

| 類別 | 技能數 | 範例 |
|------|--------|------|
| 軟體設計 | 6 | 架構模式、API 設計 |
| 軟體工程 | 7 | 測試策略、DevOps |
| 開發堆棧 | 8 | 前端、後端、資料庫 |
| 工具整合 | 6 | Git、專案管理 |
| 領域應用 | 8 | SaaS、電商、遊戲 |
| 程式語言 | 12 | TypeScript、Python、Go |

### 5.2 使用技能

在 CLAUDE.md 中引用：

```markdown
# 本專案使用的技能

@.claude/skills/software-skills/skills/software-engineering/testing-strategies/SKILL.md
@.claude/skills/software-skills/skills/development-stacks/backend/SKILL.md
```

### 5.3 搭配 Self-Evolving Agent

當 Agent 評估能力缺口時，會自動參考 software-skills：

```
能力評估：
- 需要：後端 API 開發知識
- 搜尋 skills：找到 backend/SKILL.md
- 載入並學習...
- 應用到當前任務
```

---

## Part 6: 最佳實踐

### 6.1 每日工作流程

```
開始工作
    │
    ▼
搜尋相關記憶 ─────────────┐
    │                     │
    ▼                     ▼
有相關經驗？ ──Yes──► 閱讀並應用
    │
    No
    │
    ▼
執行任務
    │
    ▼
學到新東西？ ──Yes──► 創建記憶
    │
    No
    │
    ▼
結束
```

### 6.2 記憶維護

**每月審查：**
- 合併相似的記憶
- 標註過時的為 `status: deprecated`
- 刪除不再相關的
- 更新 index.md 的 metadata

### 6.3 規則組織

```
.claude/rules/
├── global/           # 全專案規則
│   ├── code-style.md
│   └── security.md
├── frontend/         # 前端特定
│   ├── react.md
│   └── css.md
└── backend/          # 後端特定
    ├── api.md
    └── database.md
```

---

## 結語

通過本教學，你學會了：

1. **Claude Code Memory 系統**
   - 4 層記憶體架構
   - 模組化規則
   - Import 語法

2. **Self-Evolving Agent**
   - `/evolve` 觸發自動進化
   - PDCA 執行循環
   - 失敗診斷與多策略

3. **經驗記憶管理**
   - 結構化記錄經驗
   - 高效搜尋和應用
   - 持續維護和更新

4. **Skills 整合**
   - 安裝和使用技能
   - 與 Agent 協同工作

---

**下一步：**
1. Fork [claude-starter-kit](https://github.com/miles990/claude-starter-kit)
2. 根據你的需求自訂配置
3. 開始使用 `/evolve` 進行開發！
